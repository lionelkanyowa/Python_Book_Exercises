# Q.5 Will an error occur if you try to access a list element with an index greater than or equal to the list's length? For example:

foo = ['a', 'b', 'c']
print(foo[3])

#This code will result in an error because the list index is out of range.
#There is not 4th position in the list.